{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% extends 'base.html.twig' %}


{% block body %}
<div class="container form">
	<div class="row">
		<div class="col-xs-12">
			<h1>Inscription</h1>
			{{ form_start(form)}}
				{{ form_errors(form)}}

				{{ form_row(form.email)}}

				{{ form_row(form.password)}}

				{{ form_row(form.create, {'label' : 'Submit me'})}}

			{{ form_end(form)}}
		</div>
	</div>
</div>

{% endblock %}